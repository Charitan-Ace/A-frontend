import React from "react";

const updateProfileCharity = () => {
  try {
    // Add your code here
  } catch (error) {
    console.error(error);
    return null;
  }
};

export default updateProfileCharity;
